# Omitted Tests & Safeguards (Lean Sprint)

Skipped to ship in time. If demo breaks and you suspect one, retrofit it.

## Deviations from original plan (SDK reality)
- **SDK 56, not 54.** create-expo-app pulled latest. RN 0.85, React 19.2, Reanimated 4.3.1.
- **audio-studio hook = `useAudioRecorder`** (plan assumed `useExpoAudioStream`). `onAudioStream` callback is async and returns Promise. Config keys verified: `sampleRate`/`channels`/`encoding: 'pcm_16bit'`/`interval`/`onAudioStream`. `event.data` is base64 string on native.
- **moti** installed with `--legacy-peer-deps` (react/react-dom 19.2 minor mismatch in Expo template, not moti's fault). Works in bundle.
- **babel-preset-expo + expo-linking + expo-constants** had to be installed manually (blank template + expo-router peers).
- **StyleSheet.absoluteFillObject removed in RN 0.85** — replaced with explicit position:absolute insets in ResultSheet.

## Tests not written
- Component render tests (all UI). Verified via successful Metro bundle + manual.
- Navigation, gesture, animation timing tests.
- TDD covered ONLY: cosine similarity + API parsers (10 tests, pure logic). RN component tests deferred — jest-expo needs @react-native/jest-preset peer; used plain ts-jest for lib tests instead.

## Safeguards thinned
- No retry/backoff on API calls — single attempt, graceful-degrade or error sheet.
- History in-memory only (Zustand) — lost on app restart. Retrofit: zustand persist + AsyncStorage.
- File-upload source path NOW WIRED (was stubbed). Uses expo-document-picker → new File(uri).arrayBuffer() → strip 44-byte WAV header at byte level → bytesToBase64 (src/lib/base64.ts) → runScan. With mock toggle ON, returns MOCK_TRACK regardless. Native picker — Expo Go may or may not support; dev build safest.
- Real GPS replaced with static "MONTREAL" badge. Retrofit: expo-location permission + reverse geocode.
- Date-group headers (Today/Yesterday) omitted — flat list + relative timestamps.
- Matchmaking: green-flash + next-card-peek omitted, simplified to fly-off translate.
- BPM haptic poll-guard omitted — sheet renders only after stopRecording resolves, so no conflict. If haptics don't fire on device, add 300ms delay before interval.

## Hardware constraints (not bugs)
- iOS Simulator: no mic, no haptics. Live scan + haptics verify on physical iPhone only.
- Build verified via `expo export` bundle (iOS 1910 modules, web 1616 modules, both clean).

## Web runtime verification (done — Playwright headless)
- Router mounts, all 4 tabs present, Capture + Trending screens render real mock data ("Hype Boy / NewJeans / HIGH ENERGY" chips confirmed flowing through TrendingRow). Zero console errors, no red-box.
- KNOWN web-export artifact: static export mounts ALL tab screens in DOM at once, so Capture's centered absolute content overlaps Trending and intercepts clicks. On a real device (native Tabs navigator) only the active tab mounts — no overlap. NOT a device bug, but UNVERIFIED on device.
- Web CANNOT test: live mic scan (audio-studio is native-only; startRecording won't fire in headless web), haptics, gesture swipe deck.

## STILL UNVERIFIED ON DEVICE (the real test — Task 16)
- Live mic -> Shazam -> FreqBlog -> Gemini scan path (mock toggle short-circuits AFTER 5s record; real path never run).
- BPM haptic pulse.
- Matchmaking swipe gestures (gesture-handler runtime).
- Tab swap behavior (web overlaps; device should swap cleanly).
- moti animations actually animating (staggered reveal, ring pulse).

## Data (USER-OWNED — do not touch until final)
- mock_tracks.json, mock_profiles.json, sonic_profiles.json, seed.py — user handles all data at the very end. Code imports by shape; placeholders currently satisfy the contract.

## Trending charts (real data)
- Trending now reads assets/data/charts.json (real kworb Spotify charts), NOT mock_tracks.json.
- mock_tracks.json + mock_profiles.json remain as profile/matchmaking context — separate concern.
- charts.json regenerated by fetch_charts.py (kworb scrape + FreqBlog/Gemini/iTunes enrichment).
- BRITTLENESS: kworb HTML can change without notice; fetch_charts.py drops unparseable rows and exits code 2 (writing nothing) if the whole scrape fails — existing charts.json is preserved. Re-run fetch_charts.py to refresh.
- Canada daily chart is labeled "LOCAL · MONTREAL" (kworb has no city granularity).
- Album art via iTunes Search API (keyless); picsum placeholder if iTunes misses.
- Enrichment uses random feature fallback when FREQBLOG_API_KEY / OPENROUTER_API_KEY absent — chart tracks still render, values are plausible-but-fake without keys.
- kworb cell format: `Artist - Title` with space-hyphen-space. `get_text(separator=" ", strip=True)` required (plain strip=True collapses the spaces between anchor tags).
